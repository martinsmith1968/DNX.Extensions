using DNX.Extensions.Maths;
using DNX.Extensions.Maths.BuiltInTypes;
using DNX.Extensions.Tests.Maths.BuiltInTypes.TestsDataSource;
using Shouldly;
using Xunit;

//==============================================================================
// This source file was generated by a script - do not edit manually
//==============================================================================

namespace DNX.Extensions.Tests.Maths.BuiltInTypes;

public class Maths#name#ExtensionsTests
{
    [Theory]
    [MemberData(nameof(Maths#name#ExtensionsTestsDataSource.IsBetween_Default), MemberType = typeof(Maths#name#ExtensionsTestsDataSource))]
    public void IsBetween_Default(#type# value, #type# min, #type# max, bool expectedResult)
    {
        value.IsBetween(min, max).ShouldBe(expectedResult);
    }

    [Theory]
    [MemberData(nameof(Maths#name#ExtensionsTestsDataSource.IsBetween_BoundsType), MemberType = typeof(Maths#name#ExtensionsTestsDataSource))]
    public void IsBetween_BoundsType(#type# value, #type# min, #type# max, IsBetweenBoundsType boundsType, bool expectedResult)
    {
        value.IsBetween(min, max, boundsType).ShouldBe(expectedResult);
    }

    [Theory]
    [MemberData(nameof(Maths#name#ExtensionsTestsDataSource.IsBetweenEither_Default), MemberType = typeof(Maths#name#ExtensionsTestsDataSource))]
    public void IsBetweenEither_Default(#type# value, #type# min, #type# max, bool expectedResult)
    {
        value.IsBetweenEither(min, max).ShouldBe(expectedResult);
    }

    [Theory]
    [MemberData(nameof(Maths#name#ExtensionsTestsDataSource.IsBetweenEither_BoundsType), MemberType = typeof(Maths#name#ExtensionsTestsDataSource))]
    public void IsBetweenEither_BoundsType(#type# value, #type# min, #type# max, IsBetweenBoundsType boundsType, bool expectedResult)
    {
        value.IsBetweenEither(min, max, boundsType).ShouldBe(expectedResult);
    }

    [Theory]
    [MemberData(nameof(Maths#name#ExtensionsTestsDataSource.IsBetween), MemberType = typeof(Maths#name#ExtensionsTestsDataSource))]
    public void IsBetween(#type# value, #type# min, #type# max, bool allowEitherOrder, IsBetweenBoundsType boundsType, bool expectedResult)
    {
        value.IsBetween(min, max, allowEitherOrder, boundsType).ShouldBe(expectedResult);
    }

    [Theory]
    [MemberData(nameof(Maths#name#ExtensionsTestsDataSource.GetLowerBound_Default), MemberType = typeof(Maths#name#ExtensionsTestsDataSource))]
    public void GetLowerBound_Default(#type# min, #type# max, #type# expectedResult)
    {
        Maths#name#Extensions.GetLowerBound(min, max).ShouldBe(expectedResult);
    }

    [Theory]
    [MemberData(nameof(Maths#name#ExtensionsTestsDataSource.GetLowerBound_AllowEitherOrder), MemberType = typeof(Maths#name#ExtensionsTestsDataSource))]
    public void GetLowerBound_AllowEitherOrder(#type# min, #type# max, bool allowEitherOrder, #type# expectedResult)
    {
        Maths#name#Extensions.GetLowerBound(min, max, allowEitherOrder).ShouldBe(expectedResult);
    }

    [Theory]
    [MemberData(nameof(Maths#name#ExtensionsTestsDataSource.GetUpperBound_Default), MemberType = typeof(Maths#name#ExtensionsTestsDataSource))]
    public void GetUpperBound_Default(#type# min, #type# max, #type# expectedResult)
    {
        Maths#name#Extensions.GetUpperBound(min, max).ShouldBe(expectedResult);
    }

    [Theory]
    [MemberData(nameof(Maths#name#ExtensionsTestsDataSource.GetUpperBound_AllowEitherOrder), MemberType = typeof(Maths#name#ExtensionsTestsDataSource))]
    public void GetUpperBound_AllowEitherOrder(#type# min, #type# max, bool allowEitherOrder, #type# expectedResult)
    {
        Maths#name#Extensions.GetUpperBound(min, max, allowEitherOrder).ShouldBe(expectedResult);
    }
}
